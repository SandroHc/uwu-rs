The official uwu website implementation using `uwu-rs`.

For local development, please run:
```shell
# installs all dependencies
pnpm install

# replaces the `uwu-rs` version published in npm with the one locally available
pnpm link ../crates/uwu_wasm/pkg

# starts a local development server at http://localhost:5173
pnpm run dev
```
